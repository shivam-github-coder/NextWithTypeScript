import React from "react";

import { Container, Header, Main, Footer, Cards } from "@components";

const Home: React.FC = () => {
  return (
    <Container>
      <Header />
      <Cards />
    </Container>
  );
};

export default Home;
